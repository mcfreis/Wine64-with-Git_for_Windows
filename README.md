# Wine64-with-Git_for_Windows
Docker image based on Debian Bullseye mit Wine-Devel and Git_for_Windows

```shell
docker build --progress=plain -t wine64-with-git_for_windows:1.0.0 -f Dockerfile .
```
